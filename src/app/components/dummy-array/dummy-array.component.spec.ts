import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DummyArrayComponent } from './dummy-array.component';

describe('DummyArrayComponent', () => {
  // let component: DummyArrayComponent;
  // let fixture: ComponentFixture<DummyArrayComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ DummyArrayComponent ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(DummyArrayComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
